{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "go run current file",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "program": "${fileDirname}",
      "cwd": "${workspaceFolder}"
    }
  ]
}
